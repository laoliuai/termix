version: "2"
sql:
  - engine: "postgresql"
    queries: "sql/queries"
    schema: "../db/migrations"
    gen:
      go:
        package: "sqlcgen"
        out: "gen/sqlc"
        sql_package: "pgx/v5"
