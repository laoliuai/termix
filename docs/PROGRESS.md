# PROGRESS

`docs/PROGRESS.md` is the single task ledger for this repository. Add tasks when they are identified, keep incomplete work visible, and update this file before reporting completion.

## Current Milestone
Pivot from Android Compose shell to a browser-first Web UI; Android slice 2 paused at Stage A+B complete (backend ready), starting a new Web UI slice instead

Status: the host/control slice, Phase 2 relay/watch foundation, backend control lease/input loop, internal relay-control gRPC adapter, and end-to-end gRPC validation are complete. `termix-relay` now requires `TERMIX_RELAY_CONTROL_GRPC_ADDR` and no longer accepts the REST fallback. Slice 1 (`android/terminal-web/`) is implemented and manually smoke-validated against the live Go stack on 2026-04-26. Android slice 2 Stage A (backend extensions: android device_type, GET /v1/sessions, POST /v1/auth/refresh, refresh-token persistence) and Stage B (Backspace key) merged to main on 2026-04-27. The Compose shell (Stage C–G) is paused: a browser-first Web UI hits the same product loop ("随时随地查看 + 控制 host PC 上的 claude/codex session") with reuse of slice 1, faster iteration, no Android SDK install, and mobile-browser coverage out of the box. Android can be revisited later as a thin WebView host of the Web UI rather than a from-scratch Compose app.

## Completed
- [x] Choose the original spec phase sequence for delivery.
- [x] Narrow the immediate Phase 1 focus to the host/control mainline.
- [x] Approve contract-first vertical-slice implementation for Phase 1.
- [x] Define Phase 1 minimum success criteria for `login`, `start`, session registration, tmux creation, and local attach.
- [x] Approve the target repository skeleton and directory ownership.
- [x] Approve repository governance: `AGENTS.md` for rules, `docs/PROGRESS.md` for task tracking.
- [x] Write the Phase 1 host/control design document.
- [x] Update `AGENTS.md` with repository skeleton and progress-tracking rules.
- [x] Create `docs/PROGRESS.md`.
- [x] Write the Phase 1 implementation plan.
- [x] Select subagent-driven execution for the approved implementation plan.
- [x] Execute Task 1: bootstrap the monorepo skeleton and tooling.
- [x] Execute Task 2: build config, credential, and auth primitives.
- [x] Execute Task 3: define the initial PostgreSQL migrations and generated query layer.
- [x] Execute Task 4: define `openapi/control.openapi.yaml` and the generated control client.
- [x] Execute Task 5: define `proto/daemon.proto` and daemon IPC adapters.
- [x] Execute Task 6: implement `termix-control` auth and host session endpoints.
- [x] Initialize the local Git repository and add the GitHub remote.
- [x] Create the initial repository commit and push `main` to `origin`.
- [x] Adjust Codex local approval and filesystem-permission defaults.
- [x] Implement `termixd` bootstrap, local state, and tmux orchestration.
- [x] Implement thin `termix` CLI commands: `login`, `start`, `sessions attach`, `doctor`.
- [x] Add unit, integration, and smoke-test coverage for the Phase 1 slice.
- [x] Draft the Phase 2 relay/watch foundation design.
- [x] Write the Phase 2 relay/watch foundation implementation plan.
- [x] Complete Phase 2 Task 1: persist relay-capable host config during login.
- [x] Complete Phase 2 Task 2: add session detail reads for relay watch authorization.
- [x] Complete Phase 2 Task 3: define the relay protocol artifacts and Go codec layer.
- [x] Complete Phase 2 Task 4: add tmux snapshot and control-mode stream helpers.
- [x] Complete Phase 2 Task 5: add the daemon-side relay client and session publishing hooks.
- [x] Complete Phase 2 Task 6: implement the relay WSS server and watch handshake.
- [x] Complete Phase 2 Task 7: finish snapshot/live-output forwarding, verify the slice, and update the ledger.
- [x] Implement the Phase 2 relay/watch foundation.
- [x] Draft the Phase 2 control lease and remote input design.
- [x] Write the Phase 2 control lease and remote input implementation plan.
- [x] Implement the Phase 2 backend control lease and remote input slice.
- [x] Add a repo-specific Codex sandbox override so `git` commands in the `termix` workspace can write `.git` without approval prompts.
- [x] Brainstorm the internal relay-control gRPC adapter design.
- [x] Approve the internal relay-control gRPC adapter design.
- [x] Write the internal relay-control gRPC adapter implementation plan.
- [x] Implement the internal relay-control gRPC adapter for the Android backend watch/control loop.
- [x] Fix integration-test flake: randomize `tmux_session_name` in `TestCreateSessionRecord` and `TestOwnerCanFetchSessionDetailAndForeignUserCannot` so they no longer collide on the unique constraint when sharing a Postgres test database.
- [x] Validate the Phase 2 relay-control gRPC path end-to-end with a real Postgres + gRPC + relay + WSS integration test.
- [x] Remove the relay REST authorizer fallback so `termix-relay` requires the internal gRPC adapter.
- [x] Brainstorm the developer devbox container design.
- [x] Approve the developer devbox container design.
- [x] Write the developer devbox container implementation plan.
- [x] Decide to start Android control UI next, decomposed into two slices: `terminal-web` first, Compose shell second.
- [x] Brainstorm the Android slice 1 (`terminal-web` MVP) design.
- [x] Write the Android slice 1 (`terminal-web` MVP) design doc (`docs/superpowers/specs/2026-04-25-android-terminal-web-mvp-design.md`).
- [x] Write the Android slice 1 (`terminal-web` MVP) implementation plan (`docs/superpowers/plans/2026-04-25-android-terminal-web-mvp.md`).
- [x] Implement the developer devbox container under `dev/devbox/` (Ubuntu 22.04 + Go 1.25 + Node 22 LTS + Python 3.12 + uv + Go tooling + Claude Code/Codex/opencode, isolated agent state via named volumes; build args `APT_MIRROR`/`GOPROXY`/`NPM_REGISTRY`/`HOST_UID`/`HOST_GID`).
- [x] Implement Android slice 1: `terminal-web` MVP per `docs/superpowers/plans/2026-04-25-android-terminal-web-mvp.md` (Vite + TS + xterm.js bundle, JS bridge contract, WSS protocol, control-lease state machine with auto-renew, dev harness, 67 unit tests; manual smoke checklist deferred to user pre-merge per README).
- [x] Make relay WSS accept `?access_token=` query parameter as a fallback when the Authorization header is missing (browser WebSocket and Android WebView both block setting custom headers). Header still wins when both are present. Covered by two new integration tests in `relay_integration_test.go`.
- [x] Extend `accessTokenTTL` in `controlapi` from 15 minutes to 30 days for a less painful dev/smoke-test loop.
- [x] Allow cross-origin WSS handshake at the relay (`OriginPatterns: ["*"]`). Default coder/websocket Accept rejects browser/WebView origins different from Host; bearer-token auth provides the actual access control.
- [x] Treat omitted `request_id` as null in the `terminal-web` envelope decoder. The relay's Go encoder uses `json:"request_id,omitempty"`, so server-pushed envelopes (hello.ok, session.joined, ...) drop the field; the strict client validator now coalesces missing == null.
- [x] Add `android/terminal-web/scripts/smoke.sh` — single-command end-to-end orchestrator (Postgres preflight, smoke-user seed, control + relay + termixd boot, REST login, host.json patch, `termix start`, prints the four values for dev.html, tails logs until Ctrl+C). Replaces the four-shell manual workflow.
- [x] Wire live tmux output streaming end-to-end (Phase 2 backend backfill). Daemon now creates a per-session FIFO under `RunDir/output-fifos/`, asks tmux to `pipe-pane -t <session>:main.0 'stdbuf -o0 cat >> <fifo>'`, and runs a goroutine that reads from the FIFO and forwards bytes to the relay via `PublishOutput`. `stdbuf -o0` is required because libc block-buffers cat's stdout when it's writing to a non-tty. Verified end-to-end via Playwright against the live smoke stack: a marker typed in dev.html appears in the xterm panel within ~1 s (live, not just from the snapshot).
- [x] Run the manual smoke checklist in `android/terminal-web/README.md` against a live Go stack (2026-04-26). `scripts/smoke.sh` brought up Postgres, control, relay, termixd, and a `claude` session; dev.html connected over WSS, requested control (granted), sent `echo hello-from-browser`, and observed the input echoed in the xterm panel with a fresh prompt. ANSI escape sequences rendered correctly (the long inline `*.ext=NN;NN:` runs are LS_COLORS contents, not literal escape codes). Slice 1 considered shippable for the Compose host work.
- [x] Brainstorm and design Android slice 2: `android/app/` Kotlin+Compose shell. Approved design lives at `docs/superpowers/specs/2026-04-26-android-app-compose-shell-design.md` (single Gradle module, Compose + Hilt + OpenAPI-generated Kotlin Retrofit client, single-active-session navigation, reactive token refresh, three screens — Login / SessionList / Terminal — with a WebView hosting the slice-1 `terminal-web` bundle; bundles three additive backend extensions: `device_type=android`/`platform=android` enums, `GET /v1/sessions?status=running`, `POST /v1/auth/refresh`; adds `Backspace` to slice-1 `SpecialKey` enum).
- [x] Write the Android slice 2 implementation plan: `docs/superpowers/plans/2026-04-26-android-app-compose-shell.md` (21 tasks, TDD throughout; commit `22c557e`).
- [x] Slice 2 Stage A — backend API extensions (commits `9fe22ae`, `380f956`, `6bf3c29`, `1b3ec36`, `8130e24`). OpenAPI extended; android device_type accepted at login + refresh-token row persisted with SHA-256 hash; `GET /v1/sessions?status=running` (owner-scoped, status filter); `POST /v1/auth/refresh` (lookup hashed token, mint new access JWT, no rotation in V1). Includes 5 new Go tests; full suite passes against the smoke Postgres container with `TERMIX_TEST_DATABASE_URL` set.
- [x] Slice 2 Stage B — slice-1 `Backspace` key extension (commit `1b6f32d`). `terminal-web/src/protocol/types.ts` + `session/control.ts` + test row + `dev.html` button. Slice-1 suite goes 67→69 tests, all pass.
- [x] Web UI Task 1 — pivot commit (commit `ba41ddc`). Branch `web-ui` + worktree `.worktrees/web-ui/` created; plan linked.
- [x] Web UI Task 2 — OpenAPI extension (commits `5f565b5`, `649fd7a`). Widened `device_type`/`platform` enums (`web`, `ios`, `windows`); added `LoginRequest.cookie_mode`, nullable `LoginResponse.refresh_token + cookie_mode`, nullable `RefreshRequest.refresh_token`, `POST /auth/logout` stub; regenerated `go/gen/openapi/control.gen.go`.
- [x] Web UI Task 3 — Login dual-mode cookie/body (branch `web-ui`). `PostAuthLogin` now reads `cookie_mode`; when true sets `termix_refresh` HttpOnly+Secure+SameSite=Strict cookie and omits body `refresh_token`; non-cookie path unchanged. Added migration `000003_widen_device_enums` to allow `web`/`ios`/`windows` in the devices table constraints. New tests: `TestWebLoginCookieMode` (cookie assertions), `TestIosLoginEnumAccepted` (enum contract). Fixed pre-existing bug in `auth_login_android_test.go` (`*string` dereference after Task 2 changed `RefreshToken` from `string` to `*string`). 79 top-level PASS + 42 sub-tests; one pre-existing flake (`TestOwnerCanFetchSessionDetailAndForeignUserCannot` — hardcoded email collision).
- [x] Web UI Task 4 — Refresh dual-mode handler (branch `web-ui`). `PostAuthRefresh` now reads `termix_refresh` cookie first; if present and non-empty that value is used, otherwise falls back to `req.RefreshToken` in body; if neither present returns 401 "missing refresh credential". Missing-credential path changed from 400 to 401 (auth semantics). New tests: `TestRefreshUsesCookieWhenPresent` (cookie-only body → 200), `TestRefreshCookieWinsOverBody` (cookie + bogus body token → 200, cookie wins). All existing body-path refresh tests (`TestAuthRefreshHappyPath`, `TestAuthRefreshRejectsUnknownToken`, `TestAuthRefreshRejectsRevokedToken`) continue to pass; one pre-existing flake (`TestOwnerCanFetchSessionDetailAndForeignUserCannot`) unaffected.
- [x] Web UI Task 5 — Logout endpoint (branch `web-ui`). `POST /v1/auth/logout`: reads `termix_refresh` cookie, hashes it with `auth.HashRefreshToken`, calls `RevokeRefreshToken` (idempotent — only revokes un-revoked rows), always clears the cookie (`MaxAge: -1`), returns 400 if no cookie was present or 204 otherwise. Changed `RevokeRefreshToken` SQL query from `where id = $1` to `where token_hash = $1 and revoked_at is null`; regenerated `go/gen/sqlc/refresh_tokens.sql.go`; updated `persistence.Store.RevokeRefreshToken` wrapper to accept `tokenHash string`; updated the one test call-site in `auth_refresh_test.go`. Stub deleted from `server.go`; real handler in `auth_logout.go`. New tests: `TestLogoutRevokesAndClearsCookie` (204 + cookie cleared + subsequent refresh is 401), `TestLogoutWithoutCookieReturns400ButStillClears` (400 + cookie still cleared). Full suite passes; one pre-existing flake (`TestOwnerCanForeignUserCannot`) unaffected.
- [x] Web UI Task 6 — Revert access-token TTL to 30 minutes with `TERMIX_ACCESS_TOKEN_TTL` env override (branch `web-ui`). Removed the 30-day const `accessTokenTTL = 30 * 24 * time.Hour` from `go/internal/controlapi/server.go` and replaced it with a new function `auth.AccessTokenTTL()` in `go/internal/auth/tokens.go`. Function reads `TERMIX_ACCESS_TOKEN_TTL` env var (Go duration format), falls back to 30 minutes if unset or unparseable, and is called at the two issuance sites (`PostAuthLogin` and `PostAuthRefresh`). Created `go/internal/auth/tokens_test.go` with 4 new tests (default 30m, env override to 5m, bad env fallback to 30m, empty env fallback to 30m); all pass. Full Go suite: 114 tests pass, 0 failures.
- [x] Web UI Task 7 — Login rate-limit middleware (branch `web-ui`). Created `go/internal/controlapi/middleware/rate_limit_login.go` with a `LoginRateLimit(perMinute, burst int)` Gin middleware using `golang.org/x/time/rate` token-bucket per `(remote_ip, email)` key (5 req/min, burst 5, TTL-evicted after 5 min). Wired into `NewRouter` as a global middleware that only activates for `POST /api/v1/auth/login` (checked via `c.Request.URL.Path`); `/refresh` and `/logout` are not rate-limited. New test `TestLoginRateLimitBlocksAfterFiveAttempts` sends 6 requests with the same IP+email and asserts the 6th returns 429; each existing login test uses a fresh uuid-suffixed email so no cross-test bucket collision occurs. Full suite: all tests pass (one pre-existing flake `TestOwnerCanFetchSessionDetailAndForeignUserCannot` is unaffected).
- [x] Web UI Task 8 — Security headers middleware (Stage C, task 3 of 4). Created `go/internal/controlapi/middleware/security_headers.go` with a `SecurityHeaders(relayOrigin string)` Gin middleware that sets Strict-Transport-Security, Content-Security-Policy (with dynamic `connect-src` using `TERMIX_CONTROL_RELAY_ORIGIN` env var, default `wss://localhost:8090` for dev), X-Content-Type-Options, X-Frame-Options, Referrer-Policy, and Permissions-Policy headers on every response. Wired into `NewRouter` right after Recovery and before the login rate limiter (applies globally to all routes: API endpoints and future SPA static assets). New integration test `TestSecurityHeadersPresentOnResponse` verifies all headers are present and CSP includes the relay origin. Full test suite passes (all ~50 tests) except one pre-existing flake `TestOwnerCanFetchSessionDetailAndForeignUserCannot`. No regressions.
- [x] Web UI Task 9 — Optional Origin allowlist middleware (Stage C, task 4 of 4). Created `go/internal/controlapi/middleware/origin_allowlist.go` with an `OriginAllowlist(allowed string)` Gin middleware that rejects non-GET requests with an `Origin` header that doesn't match the configured allowlist. Middleware is optional: only mounted when `TERMIX_ALLOWED_ORIGIN` env var is set (empty or unset → not mounted, preserving current behavior for CLI/Compose/curl with no Origin header). Requests without an Origin header pass through unchanged (belt-and-suspenders alongside CSP + SameSite cookies). Wired conditionally into `NewRouter` after security headers. Created `go/tests/origin_allowlist_test.go` with four tests: blocked mismatched cross-origin POST, permitted matched-origin POST, allowed absent-Origin POST, and disabled-when-env-unset (cross-origin POST succeeds when middleware is not mounted). All four tests pass. Full suite: 140 tests pass, 1 pre-existing flake, 1 skipped (database integration tests skip when `TERMIX_TEST_DATABASE_URL` unset).
- [x] Web UI Task 10 — Static-asset handler with `go:embed`, `TERMIX_CONTROL_WEB_DIR` dev override, and SPA fallback (branch `web-ui`). Created `go/internal/controlapi/web_dist/` placeholder directory with `.gitkeep` and stub `index.html` (real assets land via `make build-web`). Created `go/internal/controlapi/static.go` with `StaticHandler()`: dev mode reads from `TERMIX_CONTROL_WEB_DIR` env var (disk FS), prod mode serves from `go:embed all:web_dist` subtree; non-`/assets` paths fall back to `index.html` if the file doesn't exist; `/assets/<missing>` returns 404 (no fallback, lets bundler errors surface correctly). Wired as `router.NoRoute(StaticHandler())` in `server.go` after `openapi.RegisterHandlersWithOptions` so API routes always take precedence. Created `go/tests/static_handler_test.go` with 4 new tests: dev override serves index, SPA fallback for unknown path, /assets 404 for missing file, API routes not intercepted. All 4 pass. Full suite: only pre-existing flake `TestOwnerCanFetchSessionDetailAndForeignUserCannot` fails.
- [x] Web UI Task 11 — Relay log query-string scrub helper (Stage D, task 1 of 2). Created `go/internal/relay/log_scrub.go` with `ScrubAccessTokenQuery(rawQuery string)`: uses a compiled regex to replace `access_token=...` parameter values with `<redacted>` for safe request-URL logging. Tests handle single param, siblings, URL encoding, empty value, absent token, empty string, and token at end. Created `go/tests/relay_log_scrub_test.go` with 7 subtests (single param, with siblings, URL-encoded value, empty value, no token, empty string, token at end); all 8 tests pass (parent + 7 sub). Added one-line comment in `go/internal/relay/server.go` at the point where `access_token` is read from query string, pointing future request loggers to `ScrubAccessTokenQuery` for safe logging before writing to disk. Full Go test suite: 124 tests pass, no failures. Commit staged and awaiting push.
- [x] Web UI Task 12 — Add C-j (LF, 0x0a) to slice-1 SpecialKey enum (Stage E, backend-only). Modified `android/terminal-web/src/protocol/types.ts` to add `| "C-j"` to the SpecialKey union; added encoding `"C-j": [0x0a]` to `SPECIAL_KEY_BYTES` in `android/terminal-web/src/session/control.ts`; added test row for C-j in `android/terminal-web/src/session/control.test.ts`; added `Ctrl-J` button to `android/terminal-web/dev.html`. Use case: Ctrl+J is the readline convention for "newline without submit" — high-frequency in claude/codex multi-line prompts. Slice-1 test suite: 67 → 70 tests (69 from Stage B Backspace + 1 new), all pass.
- [x] Web UI Task 13 — Relocate `android/terminal-web/` → `web/app/` (branch `web-ui`). Used `git mv android/terminal-web web/app` to preserve full history (30 file renames as single commit). Updated 7 path references in `web/app/README.md` (3 in bash examples + 2 in CLI instructions + 1 in manual section + 1 in dev server step). Updated 2 path references in `web/app/scripts/smoke.sh` (usage comment + shell instruction output). Renamed package in `web/app/package.json` from `termix-terminal-web` to `@termix/web-app`. Verified .gitignore preserved (node_modules and dist still ignored). Full smoke: npm install, npm test --run (70 passing), npm run build (produces clean dist/). Old path `android/terminal-web/` confirmed gone. All unit tests pass post-move.
- [x] Web UI Task 14 — Install Preact + Signals + preact-router + vite-plugin-pwa; rewrite `vite.config.ts`; update `tsconfig.json` for Preact JSX (branch `web-ui`). Added runtime deps: `preact`, `@preact/signals`, `preact-router`. Added dev deps: `@preact/preset-vite`, `vite-plugin-pwa`, `workbox-window`, `@testing-library/preact`, `@testing-library/jest-dom`. Rewrote `vite.config.ts` with `preact()` plugin first, `VitePWA()` second (prompt register, manifest, workbox navigate-fallback, runtimeCaching for /assets/), dev proxy for `/api/v1` → `localhost:8080` and `/ws` → `localhost:8090` (ws). Updated `tsconfig.json`: added `"jsx": "react-jsx"`, `"jsxImportSource": "preact"`, `"WebWorker"` to lib, and types array with `vite/client`, `vite-plugin-pwa/client`, `vitest/globals`, `@testing-library/jest-dom`; preserved `noUnusedLocals`, `noUnusedParameters`, `noFallthroughCasesInSwitch`, `isolatedModules`, `resolveJsonModule`, `baseUrl`/`paths`. Retained `resolve.alias` for `@` → `src/`. All 70 slice-1 tests pass. Build produces `dist/manifest.webmanifest`, `dist/sw.js`, `dist/workbox-*.js` (no icon stubs needed — PWA plugin tolerates missing icon files at build time).
- [x] Web UI Task 15 — Theme stylesheet (cream palette + dark terminal + mobile rules) (branch `web-ui`). Created `web/app/src/theme/styles.css` with CSS variables for the cream light palette (`#F5F2EA` background, `#FAF7EF` header, `#FFFFFF` card, `#1A1A1A` text) and dark mode overrides, Anthropic-orange accent (`#C96442`), dark terminal area (`#0A0A0A` bg, `#E0E0E0` text), toolbar with grid rows (digits 10-col, nav 6-col, edit 5-col, dark styling), snackbar with safe-area-inset-bottom, and mobile-responsive rules (@media 768px breakpoint for toolbar column scaling). The stylesheet defines component utility classes (`.btn-primary`, `.input-field`, `.card`, `.toolbar`, `.snackbar` and variants) ready for Task 27 import in `entry/main.tsx`. CSS is valid and complete, 133 lines.
- [x] Web UI Task 16 — Auth signal store + global app store (Stage G, task 1 of 4) (branch `web-ui`). Created `web/app/src/auth/store.ts`: `accessToken`, `accessTokenExpiresAt`, `userInfo` Preact signals; `User` and `Device` interfaces; `clearAuth()` helper. Created `web/app/src/auth/store.test.ts`: 2 Vitest tests (starts empty, clearAuth resets all). Created `web/app/src/app/store.ts`: global `snackbar` and `splashing` signals; `SnackbarState` interface with msg/kind/action; `notify()` helper. All tests pass: auth store tests 2 new + all 70 slice-1 tests = 72 passing.
- [x] Web UI Task 17 — Auth refresh single-flight (`doRefreshOnce` + `freshAccessToken`) (Stage G, task 2 of 4) (branch `web-ui`). Created `web/app/src/auth/refresh.ts`: `doRefreshOnce()` POSTs `/api/v1/auth/refresh` with single-flight coalescing (concurrent calls reuse one inflight promise); updates `accessToken` + `accessTokenExpiresAt` signals on success; returns null on failure (non-ok status or fetch throws). `freshAccessToken()` returns cached token if not within 60s of expiry (60s cushion for in-flight WSS/API requests before server-side token expires), otherwise triggers refresh. Includes fast-path optimization: if token already within 5s of expiry at entry to inflight promise, refetch rather than return marginal cache. Test-only `__resetInflight()` clears inflight state between tests. Created `web/app/src/auth/refresh.test.ts`: 6 Vitest tests (ten concurrent calls coalesce to one fetch, returns null on 401, returns null on fetch throws, freshAccessToken returns cached if not near expiry, freshAccessToken triggers refresh if within 3s of expiry—note: within 5s to force fetch—inflight cleared after resolution for sequential refreshes). All tests pass: 72 + 6 = 78 passing.
- [x] Web UI Task 18 — Cold-start cookie probe `bootstrap()` (Stage G, task 3 of 4) (branch `web-ui`). Created `web/app/src/auth/bootstrap.ts`: `bootstrap()` function runs once at `<App>` mount, POSTs to `/api/v1/auth/refresh` (cookie auto-attaches via browser fetch), returns `"authed" | "unauthed" | "network-error"`. On 200 response: parses JSON `access_token` + `expires_in_seconds`, updates `accessToken` + `accessTokenExpiresAt` signals. On 401: returns `"unauthed"`. On fetch error: returns `"network-error"`. Always sets `splashing.value = true` at entry and clears it in `finally` block to prevent UI hang. Created `web/app/src/auth/bootstrap.test.ts`: 4 Vitest tests (200 sets token + clears splash → "authed", 401 clears splash → "unauthed", fetch throws clears splash → "network-error", splash reset to true synchronously at bootstrap entry, then cleared in finally). All 4 tests pass. Full suite: 78 + 4 = 82 passing.
- [x] Web UI Task 19 — API client (`fetchWithAuth` + 401 retry) + endpoint wrappers (Stage G, task 4 of 4) (branch `web-ui`). Created `web/app/src/api/client.ts`: `fetchWithAuth(input, init)` injects `Authorization: Bearer <token>` from the `accessToken` signal; on 401 clears the stale token, calls `doRefreshOnce()` for single-flight refresh, retries once with the new token; non-401 responses pass through unchanged; no Authorization header when token is null. Created `web/app/src/api/endpoints.ts`: `login()` POSTs to `/api/v1/auth/login` with `cookie_mode=true`, `device_type=web`, `platform=web` (does NOT use `fetchWithAuth` — login produces the token); `listSessions(status)` calls `fetchWithAuth` for `/api/v1/sessions?status=<status>`, throws on non-200; `logout()` POSTs to `/api/v1/auth/logout` without Authorization header, swallows network errors. Created `client.test.ts` (5 tests) + `endpoints.test.ts` (7 tests). Full suite: 82 + 12 = 94 passing.
- [x] Web UI Task 20 — Snackbar component (signal-driven, auto-dismiss info/warn, sticky error) (branch `web-ui`). Created `web/app/src/components/snackbar.tsx`: renders nothing when `snackbar` signal is null; renders a div with `snackbar` + `kind-{info|warn|error}` classes when set; auto-dismisses (clears signal) after 3s for `info` and `warn` (using setTimeout + cleanup), stays sticky for `error`; renders an optional action button that triggers callback on click. Created `web/app/src/components/snackbar.test.tsx` with 6 tests: null renders nothing, message + kind class render when set, auto-dismiss after 3s for info, auto-dismiss after 3s for warn, no auto-dismiss for error, action button renders and callback fires on click. All 6 tests pass. Full test suite: 94 + 6 = 100 passing.
- [x] Web UI Task 21 — Toolbar component (21-key digits/nav/edit layout, button click callbacks, disable-state) (branch `web-ui`). Created `web/app/src/components/toolbar.tsx`: renders 3 rows of buttons (digits 0–9, nav Esc/Tab/arrows, edit backspace/ctrl-keys/enter) with friendly glyphs as display labels; click handlers call `onDigit(d)` or `onSpecial(k)` with canonical values; disabled state applies `is-disabled` class to wrapper and disables all buttons; `Enter` gets `key-enter` class, `C-c` and `C-d` get `key-danger` class. Created `web/app/src/components/toolbar.test.tsx` with 7 tests: 10 digit buttons render, 11 special-key buttons with glyphs render, digit click calls onDigit, special-key click calls onSpecial with canonical SpecialKey enum values (Escape/Backspace/etc), disabled state applies class + disables buttons, Enter has key-enter class, C-c/C-d have key-danger class. All 7 tests pass. Full test suite: 100 + 7 = 107 passing.
- [x] Web UI Task 22 — Composer + Header + Splash (three leaf components: composer input + send button, page header with brand/title/menu, splash loader screen + supporting CSS). Created `web/app/src/components/composer.tsx` (useSignal text binding, onSend callback, disabled state, auto-clear), `web/app/src/components/header.tsx` (brand block + title + user email + actions menu with conditional refresh + logout), `web/app/src/components/splash.tsx` (full-screen loader with brand glyph + spinner). Appended page-level CSS to `web/app/src/theme/styles.css`: 11 rules for `.page-header` + `.brand` + `.title-block` + `.actions`, 4 rules for `.composer` + `.send-btn`, 5 rules for `.splash` + `.splash-spinner` + `@keyframes spin`, 1 rule for `.snackbar-action`. Build clean; all 107 tests pass (unchanged—Task 22 doesn't add tests).
- [x] Web UI Task 23 — `useVisibility` + `useKeyboardOffset` Preact hooks (Stage H, final task). Created `web/app/src/hooks/useVisibility.ts`: calls `onShow()` when `document.visibilityState` becomes `"visible"` (Page Visibility API, useful for refetching after tab switch). Created `web/app/src/hooks/useVisibility.test.tsx`: 2 Vitest tests (calls onShow on hidden→visible flip, does NOT call onShow on visible→hidden flip). Created `web/app/src/hooks/useViewport.ts`: `useKeyboardOffset()` returns a Signal of soft-keyboard height (window.innerHeight - visualViewport.height - visualViewport.offsetTop), 0 when keyboard hidden, updates on visualViewport resize/scroll events. Created `web/app/src/hooks/useViewport.test.tsx`: 2 Vitest tests (returns 0 when full viewport, reflects keyboard offset on resize event; used direct signal reading pattern to verify synchronous updates). All 4 tests pass. Full test suite: 107 + 4 = 111 passing.
- [x] Web UI Task 24 — Login page (= landing page) with form, error states, busy-disable (Stage I, task 1 of 3). Created `web/app/src/pages/login.tsx`: email + password form with Preact Signals, calls `api.login`, writes `accessToken`/`accessTokenExpiresAt`/`userInfo` signals on success, surfaces 401/429/network-error messages inline, disables submit button while busy. Added `onClick={submit}` on the button to work around happy-dom's lack of click-to-submit propagation. Created `web/app/src/pages/login.test.tsx`: 5 Vitest tests (success writes auth signals + calls onSuccess, 401 shows "邮箱或密码错误", 429 shows "尝试过于频繁，请稍候", status=0 shows "无法连接服务器", button disabled while busy). Appended login-page CSS to `web/app/src/theme/styles.css` (`.login-page`, `.input-label`, `.form-error`, `.hint`). All 5 new tests pass; full suite: 111 + 5 = 116 passing.
- [x] Web UI Task 25 — Sessions page (Stage I, task 2 of 3). Created `web/app/src/pages/sessions.tsx`: renders list of running sessions fetched via `api.listSessions`, shows empty state ("没有正在运行的 session"), surfaces network errors via snackbar warn ("session 列表加载失败"), supports explicit refresh via header button (re-fetches), re-fetches silently on page-visibility-resume (via `useVisibility`, if >5s stale), logout flow calls `api.logout` + `clearAuth()` + `onLogout()` callback. Session rows are `<button>` elements with tool icon (first 2 chars), tool·name title, status badge, and call `onOpen(sessionId)` on click. Appended sessions CSS to `web/app/src/theme/styles.css` (`.sessions-page`, `.session-list`, `.session-row`, `.session-icon`, `.session-meta`, `.session-title`, `.session-sub`, `.session-status`, `.badge`, `.empty-state`, `.empty-glyph`, `.empty-title`, `.empty-sub`, `.page-spinner`). Created `web/app/src/pages/sessions.test.tsx`: 6 Vitest tests (empty state, rows render, row click calls onOpen, logout menu clears auth + calls callback, network error shows snackbar warn, refresh button re-fetches). All 6 new tests pass; full suite: 116 + 6 = 122 passing.

## In Progress

## Pending
- [ ] Write the Web UI implementation plan from the approved design.
- [ ] Implement the Web UI per the plan.
- [ ] Manually smoke-test the Web UI from a desktop browser AND a mobile browser against the live Go stack.
- [ ] Deferred: Android Compose shell (Stage C–G of `docs/superpowers/plans/2026-04-26-android-app-compose-shell.md`). Paused mid-slice; if revisited, will likely shrink to a thin WebView host of the Web UI plus native niceties (system back, biometrics, push) rather than the full from-scratch Compose UI in the original plan. Branch `slice-2-compose-shell` and worktree `.worktrees/android-app-slice-2/` retained as reference.
- [ ] Fix `config.DeriveHostConfig` (go/internal/config/store.go:42) so the derived `relay_ws_url` can target a different host:port from the control server. Today it copies host:port from the server base URL with the path swapped to `/ws`, which forces local dev to manually patch `host.json` after login. Likely fix: read a separate relay base URL from login response or env var.
- [ ] Add `termix sessions list` CLI subcommand. The smoke-test README references it but only `sessions attach` is implemented; users currently have to query Postgres directly to discover their session_id.
- [ ] Fix `TestOwnerCanFetchSessionDetailAndForeignUserCannot` flake: hardcoded `owner@example.com`/`other@example.com` user emails collide on repeat runs against a shared Postgres test DB. Randomize per-run via `uuid.NewString()` like the earlier `tmux_session_name` fix did.
- [ ] Fix `termix login` interactive-prompt input handling: `runLogin` calls `readLine` three times, and `readLine` (`go/cmd/termix/main.go:291`) creates a new `bufio.NewReader` per call. Piped stdin gets buffered into the first reader, then lost when it's garbage-collected, so non-TTY logins fail email-regex validation with empty values. The smoke script works around this by calling the login REST endpoint directly via curl. Fix: hoist a single bufio.Reader into `cliDeps` and reuse it across all prompts.
- [ ] Deferred: remove the now-unused REST control-lease HTTP handlers (`POST /sessions/{id}/control/{acquire,renew,release}`, `GET /sessions/{id}`) and the matching `controlapi.Client` lease/viewer methods after Android end-to-end testing confirms no REST consumer is needed.
- [ ] Deferred: implement relay-control connection lifecycle RPCs when audit or online presence is scheduled.
- [ ] Deferred: revisit `termix-admin-api` and admin Web UI after the host/control mainline when those surfaces are ready to be scheduled.

## Blocked
- [ ] No active blockers.

## Next Up
1. Brainstorm + design the Web UI in a fresh session.
2. Write the Web UI implementation plan.
3. Implement the Web UI; smoke-test from desktop + mobile browsers against the live Go stack.
4. Decide whether to resume the Android Compose shell or keep it deferred (likely deferred and replaced by a thin WebView host of the Web UI).
5. Deferred: remove the REST control-lease HTTP surface once end-to-end testing confirms no REST consumer.
6. Deferred: revisit `termix-admin-api` and admin Web UI when ready.
